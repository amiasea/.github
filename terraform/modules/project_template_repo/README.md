## Features

- **Template Mode**: Enabled via `is_template = true` to allow "Use this template" functionality in the GitHub UI.
- **Advanced Security**: Secret scanning and advanced security features are enabled by default.
- **Auto-Init**: Bootstraps the repo with an initial commit and README.